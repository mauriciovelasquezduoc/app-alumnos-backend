apiVersion: v1
kind: Service
metadata:
  name: app-alumnos-backend
  namespace: app-alumnos
spec:
  selector:
    app: app-alumnos-backend
  ports:
    - port: 8080
      targetPort: 8080
  type: ClusterIP
