apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: app-alumnos-backend-hpa
  namespace: app-alumnos
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: app-alumnos-backend
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70